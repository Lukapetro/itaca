pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{ts,js,json}"
      run: bunx biome check --write --no-errors-on-unmatched {staged_files} && git update-index --again
    - name: typecheck
      glob: "*.ts"
      run: bun run typecheck
